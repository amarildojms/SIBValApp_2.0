/// Opções compartilhadas dos campos "Forma de Adesão" (`Member`, dado
/// eclesiástico — cadastro manual de membro em `members_page.dart` e edição
/// de perfil em `edit_profile_page.dart`) e "Estado civil" (`AppUser`, dado
/// pessoal desde 29/08/2026 — só `edit_profile_page.dart`). Extraídas para
/// não duplicar a mesma lista em mais de uma tela.
const admissionFormOptions = ['Batismo', 'Transferência', 'Aclamação', 'Reconciliação'];

const maritalStatusOptions = ['Solteiro(a)', 'Casado(a)', 'Divorciado(a)', 'Viúvo(a)', 'União estável'];
